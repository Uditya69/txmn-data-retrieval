[project]
name = "web"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
  "streamlit>=1.38",
  "websockets>=13.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/web"]
